name: CI

on:
  push:
  pull_request:

jobs:
  lint:
    name: Lint (ruff)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install package with dev extras
        run: pip install -e ".[dev]"
      - name: ruff check
        run: ruff check src tests problem_sets capstone scripts
      - name: ruff format --check
        run: ruff format --check src tests problem_sets capstone scripts

  unit:
    name: Unit tests
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package with dev extras
        run: pip install -e ".[dev]"
      - name: Run pytest with coverage (offline; no network)
        run: pytest -q --cov=ddm4bio --cov-report=term-missing --cov-fail-under=85

  # notebooks-fast:
  #   # Manual-only placeholder. Credentialed / network dataset jobs are kept
  #   # OUT of default CI. Run this manually via workflow_dispatch when needed.
  #   # No secrets are used or provisioned here.
  #   name: Notebooks (fast, manual)
  #   if: ${{ github.event_name == 'workflow_dispatch' }}
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-python@v5
  #       with:
  #         python-version: "3.11"
  #     - run: pip install -e ".[dev]"
  #     - run: echo "Run fast notebook smoke tests here (offline subset only)."
